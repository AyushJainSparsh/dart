void main()
{
  // if we use var then dart automatically understand the data type and you can't reinitialise another datatype in it
  var a = 10;//Here a automatically intialise as int
  // dart use int float double String bool data type
  // it also define list as an array
  // sets , maps (dictionary in python) 
  int x = 10;
  print(a);  print(x);
  a = 5;
}